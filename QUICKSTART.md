# Быстрый старт (Quick Start)

Хотите сразу начать практиковаться с конфликтами слияния? Следуйте этим шагам!

## Шаг 1: Клонируйте репозиторий

```bash
git clone https://github.com/viktorplus/lesson_git.git
cd lesson_git
```

## Шаг 1.5: Настройка (если ветки отсутствуют)

Если при выполнении `git branch -a` вы не видите веток `feature-*`, запустите скрипт настройки:

```bash
chmod +x setup.sh
./setup.sh
```

Этот скрипт создаст все необходимые feature-ветки локально.

## Шаг 2: Просмотрите доступные ветки

```bash
git branch -a
```

Вы увидите несколько веток с префиксом `feature-`. Это ветки для практики.

## Шаг 3: Ваш первый конфликт (5 минут)

```bash
# Создайте ветку для практики
git checkout -b my-first-merge

# Слейте первую feature ветку (это пройдет без конфликта)
git merge feature-hello-enthusiastic

# Теперь слейте вторую - здесь будет конфликт!
git merge feature-hello-formal
```

### Что вы увидите:

```
Auto-merging hello.txt
CONFLICT (content): Merge conflict in hello.txt
Automatic merge failed; fix conflicts and then commit the result.
```

## Шаг 4: Откройте файл с конфликтом

```bash
cat hello.txt
```

Вы увидите маркеры конфликта:

```
<<<<<<< HEAD
Hello Amazing World!!!
...
Line 5: Have a wonderful day!
=======
Greetings, World.
...
Line 5: Best regards
>>>>>>> feature-hello-formal
```

## Шаг 5: Разрешите конфликт

Откройте `hello.txt` в любом текстовом редакторе и:

1. Удалите маркеры `<<<<<<<`, `=======`, `>>>>>>>`
2. Выберите какую версию оставить, или объедините обе
3. Сохраните файл

Например:
```
Hello World!
This is a file for practicing Git merge conflicts.
Line 3: Professional and exciting content
Line 4: More original content
Line 5: Best regards and have a wonderful day!
```

## Шаг 6: Завершите слияние

```bash
# Пометьте конфликт как разрешенный
git add hello.txt

# Проверьте статус
git status

# Завершите слияние
git commit -m "Resolved merge conflict in hello.txt"
```

🎉 **Поздравляем!** Вы успешно разрешили свой первый конфликт слияния!

## Что дальше?

1. Попробуйте другие сценарии из `README.md`
2. Изучите `CONFLICTS_GUIDE.md` для более сложных примеров
3. Практикуйтесь с конфликтами в коде (`feature-calc-*`)
4. Попробуйте конфликты в JSON (`feature-config-*`)

## Полезные команды для практики

```bash
# Отменить текущее слияние и начать заново
git merge --abort

# Посмотреть визуализацию веток
git log --graph --oneline --all --decorate

# Посмотреть содержимое файла из определенной ветки
git show feature-hello-enthusiastic:hello.txt

# Принять полностью их версию
git checkout --theirs hello.txt

# Принять полностью вашу версию
git checkout --ours hello.txt
```

## Советы для новичков

✅ **Делайте:** 
- Читайте весь конфликт перед разрешением
- Проверяйте что код работает после разрешения
- Используйте `git status` чтобы видеть что происходит

❌ **Не делайте:**
- Не паникуйте! Конфликты - это нормально
- Не удаляйте код не разобравшись
- Не оставляйте маркеры конфликта в коде

## Часто задаваемые вопросы

**Q: Я застрял в слиянии. Как выйти?**  
A: `git merge --abort` отменит слияние

**Q: Как увидеть что изменилось в ветке?**  
A: `git diff feature-hello-enthusiastic`

**Q: Могу ли я попробовать снова?**  
A: Да! Просто создайте новую ветку и повторите процесс

**Q: Что если я случайно сделал коммит с маркерами конфликта?**  
A: Не волнуйтесь, можно отредактировать последний коммит: `git commit --amend`

## Проверьте себя

После первой практики, вы должны понимать:
- [ ] Что означают маркеры `<<<<<<<`, `=======`, `>>>>>>>`
- [ ] Как отменить слияние
- [ ] Как пометить конфликт как разрешенный
- [ ] Как завершить слияние после разрешения конфликтов

Удачи в изучении Git! 🚀
